; supervisor config file

[program:vysualWS]
command                 = bash -c "ulimit -n 10000; exec /usr/bin/php ./var/www/html/vysual/websocket/server.php"
process_name            = vysualWS
numprocs                = 1
autostart               = true
autorestart             = true
user                    = root
stdout_logfile          = /var/log/vysualWS_info.log
stdout_logfile_maxbytes = 1MB
stderr_logfile          = /var/log/vysualWS_error.log
stderr_logfile_maxbytes = 1MB

[program:vysualWSClient]
command = bash -c "ulimit -n 10000; exec nodejs ./var/www/html/vysual/websocket/vysualWebsocketClient.js  --dir /var/www/html/vysual/websocket/"
process_name = vysualWSClient
numprocs = 1
autostart = true
autorestart = true
user = root
stdout_logfile = /var/log/vysualWSClient_info.log
stdout_logfile_maxbytes = 1MB
stderr_logfile = /var/log/vysualWSClient_error.log
stderr_logfile_maxbytes = 1MB

[program:vysualCore]
command                 = bash -c "ulimit -n 10000; exec ./opt/vysual/vysuald"
process_name            = vysualCore
numprocs                = 1
autostart               = true
autorestart             = true
user                    = root
stdout_logfile          = /var/log/vysualCore_info.log
stdout_logfile_maxbytes = 1MB
stderr_logfile          = /var/log/vysualCore_error.log
stderr_logfile_maxbytes = 1MB


[unix_http_server]
file=/var/run/supervisor.sock   ; (the path to the socket file)
chmod=0700                       ; sockef file mode (default 0700)

[supervisord]
logfile=/var/log/supervisor/supervisord.log ; (main log file;default $CWD/supervisord.log)
pidfile=/var/run/supervisord.pid ; (supervisord pidfile;default supervisord.pid)
childlogdir=/var/log/supervisor            ; ('AUTO' child log dir, default $TEMP)

; the below section must remain in the config file for RPC
; (supervisorctl/web interface) to work, additional interfaces may be
; added by defining them in separate rpcinterface: sections
[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock ; use a unix:// URL  for a unix socket

; The [include] section can just contain the "files" setting.  This
; setting can list multiple files (separated by whitespace or
; newlines).  It can also contain wildcards.  The filenames are
; interpreted as relative to this file.  Included files *cannot*
; include files themselves.

[include]
files = /etc/supervisor/conf.d/*.conf
